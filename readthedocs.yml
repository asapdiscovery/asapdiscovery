# readthedocs.yml

version: 2

build:
  image: latest
  tools:
    python: 3.10

sphinx:
   configuration: docs/conf.py

python:
  version: 3.10
  install:
    - method: pip
      path: .

conda:
  environment: docs/requirements.yaml
